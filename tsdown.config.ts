import { defineConfig } from 'tsdown'

/** Build the plugin and invariant entry points with their shared DSH-native tools. */
export default defineConfig({
  entry: ['src/index.ts', 'src/invariant.ts'],
  outDir: 'lib',
  format: ['esm'],
  platform: 'node',
  target: 'es2024',
  fixedExtension: false,
  dts: false,
  clean: true,
})
